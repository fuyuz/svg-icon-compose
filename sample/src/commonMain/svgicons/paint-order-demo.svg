<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
  <!-- Circle with default paint-order (fill first, stroke on top) -->
  <circle cx="7" cy="7" r="4" fill="currentColor"/>

  <!-- Circle with stroke painted first, fill on top (paint-order: stroke) -->
  <circle cx="17" cy="7" r="4" fill="currentColor" paint-order="stroke"/>

  <!-- Rect with default paint-order -->
  <rect x="3" y="13" width="8" height="8" rx="1" fill="currentColor"/>

  <!-- Rect with stroke painted first (paint-order: stroke fill) -->
  <rect x="13" y="13" width="8" height="8" rx="1" fill="currentColor" paint-order="stroke fill"/>
</svg>
